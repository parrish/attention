language: ruby
sudo: false
cache: bundler
rvm:
  - 2.2.3
  - jruby-9.0.1.0
before_install: gem install bundler -v 1.11.2
services:
  - redis-server
jruby_before_script:
  - bundle exec jbundle install
